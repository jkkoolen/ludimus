logging:
  level:
    org:
      hibernate:
        type: error
  file: ludimus-debug.log

management:
  endpoints:
    web:
      exposure:
        include: info, health, httptrace

google:
  mapsUrl: https://maps.googleapis.com/maps/api/distancematrix/json

jedis:
  pool:
    host: redis://:@localhost:6379/

ludimus:
  dataDir: /Users/jkkoolen/data